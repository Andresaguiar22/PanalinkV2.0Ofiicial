kotlin.daemon.jvmargs=-Xmx4g
org.gradle.jvmargs=-Xmx4g

# NOTA: no anadir aqui rutas absolutas del entorno local ni conmutadores tipo
# sslVerify=false. Rompen la reproducibilidad del build entre maquinas y
# desactivan la verificacion TLS de Gradle. Si un entorno concreto necesita un
# trustStore, definelo fuera del repo (JAVA_HOME / JAVA_TOOL_OPTIONS / variables
# de CI), no en este fichero versionado.
